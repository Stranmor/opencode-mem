Понял: требуется улучшение архитектуры. В AGENTS.md уже зафиксирован список критического архитектурного долга, который включает нарушения SPOT и проблемы абстракций. Я беру в работу исправление этих фундаментальных дефектов.

План:
1. Устранить обход абстракции в `SearchService` (используется дублирование вместо структуры `HybridSearch`).
2. Устранить нарушение SPOT для `ObservationType` (сейчас 8 вариантов в Rust, 4 в промптах, 6 в схемах MCP).
3. Исправить рассинхронизацию состояния конфигурации (`Settings state ignored`).
4. Починить узкое место и формат UUID в `QueueProcessor`.
